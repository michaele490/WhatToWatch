<!--<h1>Add New TV Show</h1>-->

<%#= render 'form', tv_show: @tv_show %>
<%#= button_to 'Back', tv_shows_path, method: :get, class: 'return-btn' %>

<div class="container">
  <div class="row">
    <div class="col-12 d-flex justify-content-between align-items-center">
      <%= button_to 'Back', tv_shows_path, method: :get, class: 'return-btn' %>
      <!--<h1>Add New TV Show</h1>-->
      <div class="flex-grow-1 text-center"> <h1>Add New TV Show</h1> </div>
    </div>
  </div>
  <div class="row">
    <div class="col-12">
      <%= render 'form', tv_show: @tv_show %>
    </div>
  </div>
</div>